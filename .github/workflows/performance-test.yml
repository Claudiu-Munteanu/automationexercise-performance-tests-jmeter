name: JMeter Performance Test

on:
  workflow_dispatch:

jobs:
  run-jmeter-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Cache JMeter
        id: jmeter-cache
        uses: actions/cache@v4
        with:
          path: apache-jmeter-5.6.3
          key: ${{ runner.os }}-jmeter-5.6.3

      - name: Setup JMeter
        if: steps.jmeter-cache.outputs.cache-hit != 'true'
        run: |
          JMETER_VERSION="5.6.3"
          wget https://archive.apache.org/dist/jmeter/binaries/apache-jmeter-${JMETER_VERSION}.tgz
          tar -xzf apache-jmeter-${JMETER_VERSION}.tgz
          echo "JMETER_HOME=$PWD/apache-jmeter-${JMETER_VERSION}" >> $GITHUB_ENV

      - name: Set JMETER_HOME from Cache
        if: steps.jmeter-cache.outputs.cache-hit == 'true'
        run: echo "JMETER_HOME=$PWD/apache-jmeter-5.6.3" >> $GITHUB_ENV

      - name: Run JMeter Test
        run: |
          rm -rf target/
          mkdir -p target/reports
          
          $JMETER_HOME/bin/jmeter \
            -n \
            -t "src/test/jmeter/Automation Exercise - Performance Tests - JMeter.jmx" \
            -l "target/results/test-results.jtl" \
            -e \
            -o "target/reports"

      - name: Upload Test Report
        uses: actions/upload-artifact@v4
        with:
          name: jmeter-html-report
          path: target/reports/